apiVersion: v1
kind: Service
metadata:
  name: portfolio-svc
  namespace: production
  labels:
    app: portfolio
spec:
  selector:
    app: portfolio
  ports:
  - name: http
    port: 80
    targetPort: 3000
    protocol: TCP
  type: LoadBalancer   # E2E Cloud provisions a public IP for this
